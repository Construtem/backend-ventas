apiVersion: v1
kind: Service
metadata:
  name: backend-ventas-service
spec:
  selector:
    app: backend-ventas
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
