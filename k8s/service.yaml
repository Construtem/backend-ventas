apiVersion: v1
kind: Service
metadata:
  name: back-ventas-service
spec:
  type: ClusterIP
  selector:
    app: back-ventas
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
